---

services:
  openhands:
    image: ghcr.io/openhands/agent-canvas:latest
    container_name: openhands
    restart: always
    ports:
      - "3000:8000"
    volumes:
      - "${TESSERACT_APPDATA_PATH}/openhands_data:/home/openhands/.openhands"
